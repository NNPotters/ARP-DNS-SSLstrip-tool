import scapy.all as scapy

scapy.lsc() # list of scapy commands

#scapy.arping() #Send ARP who-has requests to determine which hosts are up, needs 'net' argument
#scapy.arpcachepoison() # poison target's ARP cache
#scapy.arp_mitm() # poison 2 target's ARP cache
